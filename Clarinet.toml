[project]
name = "proven-priority-distributor"
authors = ["Anonymous"]
description = "A decentralized priority distribution mechanism for secure resource allocation"
telemetry = false
requirements = []
[contracts.priority-distributor]
path = "contracts/priority-distributor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
